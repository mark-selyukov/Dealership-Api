{{- if .Values.mongoSecret.create}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.mongoSecret.secretName}}
type: Opaque
data:
  {{ .Values.mongoSecret.key }}: mongodb://{{.Values.mongodb.auth.username}}:{{.Values.mongodb.auth.password}}@{{.Values.mongoSecret.databaseName}}
{{- end}}